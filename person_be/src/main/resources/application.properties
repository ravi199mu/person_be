
spring.application.name=person-be
server.port=8010

#database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/springpractice
spring.datasource.username=ravi
spring.datasource.password=ravi

#register fe service to discovery server 
eureka.client.serviceUrl.defaultZone=http://localhost:8000/eureka

eureka.client.register-with-eureka=false

eureka.client.fetch-registry=false




spring.servlet.multipart.enabled=true

# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB

# Max file size.
spring.servlet.multipart.max-file-size=200MB

# Max Request Size
spring.servlet.multipart.max-request-size=215MB

# All files uploaded through the REST API will be stored in this directory
#file.upload-dir=/home/ravi/media/upload






